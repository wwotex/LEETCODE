from typing import List



class Solution:
    def jump(self, nums: List[int]) -> int:
        steps = [-1 for el in nums]
        best_stamina[]
        for i in range(0, len(nums)):
            if nums[i] > best_stamina:
                best_stamina = nums

        print(steps)
        return steps[-1]        

sol = Solution()
nums = [2,0,2,3,4]
print(sol.jump(nums))